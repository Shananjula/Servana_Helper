// lib/screens/post_task_screen.dart
import 'package:flutter/material.dart';
class PostTaskScreen extends StatelessWidget {
  const PostTaskScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Post task')), body: const Center(child: Text('Not available in Helper app.')));
  }
}
